# 今天穿什么

这是一个纯静态前端页面，可以直接部署到 Vercel、Netlify、GitHub Pages，或任何静态网站托管服务。

## 文件

- `index.html`：页面结构
- `styles.css`：视觉样式
- `app.js`：衣柜管理、标签、穿搭推荐、穿搭图片风格学习逻辑

## 部署

把当前文件夹 `today-outfit` 作为静态站点根目录上传即可，不需要安装依赖，也不需要构建命令。

